pyrogram
tgcrypto
pyromod==1.4

# added for /MS (promo/watermark text removal on video covers)
opencv-python-headless
numpy
pytesseract
# pytesseract needs the Tesseract OCR system binary installed separately,
# it is NOT a pip package:
#   Debian/Ubuntu/VPS: sudo apt-get install -y tesseract-ocr
#   (no ffmpeg binary needed — cv2.VideoCapture reads the video frame directly)
